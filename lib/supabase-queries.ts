/**
 * @deprecated 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새 코드에서는 @/lib/queries 또는 @/lib/queries/[domain] 을 직접 사용하세요.
 *
 * 예시:
 *   import { getCitiesList } from "@/lib/queries/cities";
 *   import { getUserRating } from "@/lib/queries/user-ratings";
 *   import { getCityReviews } from "@/lib/queries/user-reviews";
 *   import { getWorkspacesByCity } from "@/lib/queries/workspaces";
 *   import { getWorkspaceReviews } from "@/lib/queries/workspace-reviews";
 */
export * from "@/lib/queries";
